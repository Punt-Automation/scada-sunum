# SCADA / MES Ürün Sunumu

Yem fabrikası otomasyonu tanıtım sayfası. Yalnızca ekran görüntüleri ve açıklama metni içerir; uygulama kaynak kodu bu repoda **yoktur**.

## Canlı site

**https://once80-svg.github.io/scada-sunum/**

(İlk yayın sonrası 1–2 dakika sürebilir.)

## Güncelleme

1. Private projedeki `Sunum` klasörünü düzenle.
2. Bu repoya kopyala: `index.html` + `Photos/` (HTML içindeki görsel yolları `Photos/...` olmalı).
3. `git add -A && git commit --trailer "Co-authored-by: Cursor <cursoragent@cursor.com>" -m "Update brochure" && git push`

Kaynak kod: private `web-scada` reposunda kalır; bu repo public brochure içindir.
